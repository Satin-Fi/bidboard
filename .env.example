# Frontend env. Leave blank for same-origin (Vercel + serverless proxy, or local dev proxy).
# For a standalone backend, set both to the backend URL:
# VITE_API_URL=https://api.bidboard.app
# VITE_WS_URL=wss://api.bidboard.app
VITE_API_URL=
VITE_WS_URL=
